import { normalizeImagePath } from 'rspress/runtime';

# 🖼️ 插屏广告

:::tip{title="💡 小提示"}

 - `全插屏、半插屏、横插屏`都是在 GroMore 后台配置
 - [参考官方文档](https://www.csjplatform.com/union/media/union/download/detail?id=142&docId=27618&osType=android)
 - Pro 版客户，如有问题随时联系我，提供配置指导

:::

## 演示视频

<video src={normalizeImagePath('/imgs/view/video_insert.mp4')}  width='260px' controls />

## 全插屏

```dart title="example/pages/fullscreen_video_page.dart"
/// 就是全屏视频广告
/// [posId] 广告位 id
FlutterGromoreAds.showInterstitialAd(posId);
```

## 半插屏

```dart title="example/pages/fullscreen_video_page.dart"
/// 类似于弹窗广告
/// [posId] 广告位 id
FlutterGromoreAds.showInterstitialAd(posId);
```

## 横插屏

```dart title="example/pages/fullscreen_video_page.dart"
/// 这里要确保手机是横屏的，可旋转横屏的
/// [posId] 广告位 id
FlutterGromoreAds.showInterstitialAd(posId);
```
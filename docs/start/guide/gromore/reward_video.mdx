import { normalizeImagePath } from 'rspress/runtime';

# 🏆 激励视频(Pro)

:::tip{title="💡 小提示"}

 - `二次激励视频` 需在 GroMore 后台配置
 - [参考官方文档](https://www.csjplatform.com/union/media/union/download/detail?id=142&docId=27617&osType=android)
 - `请使用真机测试，不要使用模拟器`
 - Pro 版客户，如有问题随时联系我，提供配置指导

:::

## 演示视频

<video src={normalizeImagePath('/imgs/view/video_reward.mp4')}  width='260px' controls />

## 展示广告

```dart title="example/pages/reward_video_page.dart"
/// [posId] 广告位 id
/// [customData] 设置服务端验证的自定义信息
/// [userId] 设置服务端验证的用户信息
bool result = await FlutterGromoreAds.showRewardVideoAd(
    posId,
    customData: 'customData',
    userId: 'userId',
);
```

## 激励事件监听

> 如果在[快速开始](../go)章节跳过了[广告监听](../go#广告监听)，那么可以回去看看注释中的`获得广告激励事件`

```dart title="flutter_gromore_ads/event/ad_reward_event.dart"
// 参考类  AdRewardEvent
// 奖励类型，0:基础奖励 >0:进阶奖励，v3.0.0 新增
final int rewardType;
// 奖励是否有效
final bool rewardVerify;
// 奖励数量
final int rewardAmount;
// 奖励名称
final String rewardName;
// 错误码
final int? errCode;
// 错误信息
final String? errMsg;
// 服务端验证的自定义信息
final String? customData;
// 服务端验证的用户信息
final String? userId;
```

## 服务端奖励验证

一般不需要验证，在一些比如金币、积分、提现、兑换等场景则需要确定激励视频被真正的看了而非接口被刷，则需要增加服务端验证增加安全性。

- [详细参考官方文档](https://www.csjplatform.com/union/media/union/download/detail?id=142&docId=27623&osType=android)
